<UserControl x:Class="BroPilot.ModelsWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BroPilot.ViewModels"
             xmlns:converters="clr-namespace:BroPilot.Converters"
             xmlns:wpf="clr-namespace:BroPilot.Wpf"
             xmlns:md="http://schemas.lepo.co/wpfui/2022/xaml/markdown"
             mc:Ignorable="d" 
              Unloaded="UserControl_Unloaded"
             d:DesignHeight="600" d:DesignWidth="700"
             d:DataContext="{d:DesignInstance Type=local:ModelsViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <converters:NullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisibilityConverter"/>
            <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{StaticResource GrayPurpleGradient}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!--Header-->
            <Grid Margin="5" VerticalAlignment="Center">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0"
                        Height="30"
                        VerticalAlignment="Center"
                        Margin="5" />
                <Image Source="/BroPilot;component/__..___001_small.png" />
                <TextBlock Grid.Column="1"
                       Text="Model configuration" 
                       FontSize="18"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Left"
                       TextTrimming="CharacterEllipsis"
                       TextWrapping="NoWrap"
                       Margin="5" />
            </Grid>

            <!--Main-->
            <Grid Grid.Row="2">
                    <DockPanel Margin="10">

                        <!-- Add button at the top -->
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
                        <Button Content="Add Model"
                                Width="100"
                                Command="{Binding AddModelCommand}" />      
                    </StackPanel>

                        <!-- List of agents -->
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl ItemsSource="{Binding Models}" Margin="0,0,15,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                    <Grid >
                                        
                                        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10" Padding="10" >
                                            <StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                    <TextBlock Text="Name:" Width="100" VerticalAlignment="Center"/>
                                                <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="350" FontSize="12"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                    <TextBlock Text="Model:" Width="100" VerticalAlignment="Center"/>
                                                    <TextBox Text="{Binding ModelName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="350" FontSize="12"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                    <TextBlock Text="Address:" Width="100" VerticalAlignment="Center"/>
                                                    <TextBox Text="{Binding Address, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="350" FontSize="12"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                    <TextBlock Text="Temperature:" Width="100" VerticalAlignment="Center"/>
                                                    <TextBox Text="{Binding Temperature, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" FontSize="12"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,10,10">
                                                    <TextBlock Text="Max Tokens:" Width="100" VerticalAlignment="Center"/>
                                                    <TextBox Text="{Binding MaxTokens, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" FontSize="12"/>
                                                </StackPanel>

                                            </StackPanel>
                                        </Border>

                                        <!-- Delete button -->
                                        <Button Content="🗑️Remove model"
                                        HorizontalAlignment="Right" VerticalAlignment="Top"
                                        Command="{Binding DataContext.DeleteModelCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" 
                                        Width="Auto" Margin="5"/>
                                    </Grid>

                                </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </DockPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>